/*
 * test.c
 *
 *  Created on: Nov 6, 2024
 *      Author: hellm
 */

#include "test.h"

typedef struct testStructure_t {

	testStatus_t ResultState;
	uint8_t *pcTestFunctionName;
	uint16_t sTestFunctionNum;
	uint16_t sTimeSpentOnTest;
	uint8_t cbuffer[255];
}testStructure_t;

testStatus_t exempleTestFunctionPattern( testStructure_t *ptLocalTestStructure)
{
	// do a ptit test
	return 0;
}

void TFunction_Factory(void){

	#define NUM_TFUNCT 1

	testStructure_t testStructure;
	uint8_t (*TabOfTFunction[NUM_TFUNCT])( testStructure_t *ptTestStructure) = {
			exempleTestFunctionPattern,
		//	TFunction_2,
        //  TFunction_3,
			};

	for(int i =0; i<NUM_TFUNCT; i++)
	{
		if( TabOfTFunction[i](&testStructure) )
		{
			printf("result%d", ptTestStructure-> );
		}

	}
}
